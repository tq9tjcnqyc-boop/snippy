[package]
name = "snippy"
version = "0.1.0"
edition = "2024"

[dependencies]
rdev = "0.5"
enigo = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[profile.release]
opt-level = "z"
lto = "thin"
strip = true
